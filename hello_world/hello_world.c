# include <stdio.h> //this gives the ability to use printf

int main() {
    printf("Hello World!\n")
    // \n is the "new line" character, it's the same thing as hitting "enter" on your keyboard while typing.
    return 0
    // tells the operating system that the program ran successfully
}
